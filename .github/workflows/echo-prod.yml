name: echo action
on:
  workflow_dispatch:
  push:
    branches: [ env/prod ]
jobs:
  echo-job-main:
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - name: Run a script on prod
        run: |
          echo Hello, world!
